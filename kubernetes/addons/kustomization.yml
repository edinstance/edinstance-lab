apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # cert-manager and metallb controllers remain bootstrapped outside this Flux
  # addons set; see gitops.md and helmfile.yaml. Certificate resources are
  # reconciled by the separate Flux certificates Kustomization.
  - cloudflare-tunnel
  - cloudnative-pg
  - envoy-gateway
  - observability
  - opentelemetry
  - loki
  - alloy
  - tempo
