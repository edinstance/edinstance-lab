# App Template

Copy this folder when manually adding a service before the platform API exists.
The custom control plane should eventually generate equivalent resources from an
`App` object.

Replace:

```text
SERVICE_NAME
SERVICE_IMAGE
SERVICE_PORT
SERVICE_HOST_LOCAL
```
