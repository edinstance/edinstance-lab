apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: edinstance-lab
  namespace: flux-system
spec:
  interval: 1m
  url: ssh://git@github.com/edinstance/edinstance-lab.git
  ref:
    branch: main
  secretRef:
    name: flux-system
