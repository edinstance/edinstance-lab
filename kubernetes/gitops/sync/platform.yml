apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: platform
  namespace: flux-system
spec:
  dependsOn:
    - name: addons
  interval: 10m
  path: ./kubernetes/platform/crds
  prune: true
  sourceRef:
    kind: GitRepository
    name: edinstance-lab
  timeout: 5m
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
